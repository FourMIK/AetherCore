[package]
name = "aethercore-jni"
version = "0.1.0"
edition = "2021"

[lib]
name = "aethercore_jni"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tokio = { version = "1.0", features = ["rt-multi-thread", "sync", "time"] }
tonic = "0.10"
prost = "0.12"
blake3 = "1.5"
once_cell = "1.19"

# AetherCore crates
aethercore-identity = { path = "../../crates/identity", features = ["grpc-server"] }
aethercore-crypto = { path = "../../crates/crypto" }

[workspace]
