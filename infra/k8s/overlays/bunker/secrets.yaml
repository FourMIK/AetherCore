# secrets.yaml - Template for Kubernetes secrets
# 
# ⚠️  CRITICAL SECURITY WARNING ⚠️
# This file contains PLACEHOLDER values that will BREAK your deployment.
# DO NOT use this file directly in production.
# 
# REQUIRED ACTIONS BEFORE DEPLOYMENT:
#   1. Create secrets.env from secrets.env.example
#   2. Fill in ALL values with secure random strings
#   3. Uncomment secretGenerator in kustomization.yaml
#   4. Deploy: kustomize build . | kubectl apply -f -
#
# OR use sealed-secrets for production:
#   https://github.com/bitnami-labs/sealed-secrets
#
# Deployment will FAIL if these placeholder values are detected.

apiVersion: v1
kind: Secret
metadata:
  name: bunker-secrets-template
type: Opaque
stringData:
  # ⚠️ PLACEHOLDER - Replace before deployment
  POSTGRES_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY_OR_DEPLOYMENT_FAILS"
  
  # ⚠️ PLACEHOLDER - Replace before deployment
  JWT_SECRET: "CHANGE_ME_BEFORE_DEPLOY_OR_DEPLOYMENT_FAILS"
  
  # ⚠️ PLACEHOLDER - Replace before deployment
  MINIO_ROOT_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY_OR_DEPLOYMENT_FAILS"
  
  # ⚠️ PLACEHOLDER - Replace before deployment
  GRAFANA_ADMIN_PASSWORD: "CHANGE_ME_BEFORE_DEPLOY_OR_DEPLOYMENT_FAILS"
  
  # ⚠️ PLACEHOLDER - Replace before deployment
  DATABASE_URL: "postgresql://aethercore:CHANGE_ME_BEFORE_DEPLOY@postgres:5432/aethercore"
---
# TLS Certificate Secret (generated by cert-manager or manually)
apiVersion: v1
kind: Secret
metadata:
  name: tactical-glass-tls
type: kubernetes.io/tls
stringData:
  tls.crt: |
    -----BEGIN CERTIFICATE-----
    # Replace with actual certificate
    # Generated by scripts/generate-tactical-certs.sh or cert-manager
    -----END CERTIFICATE-----
  tls.key: |
    -----BEGIN PRIVATE KEY-----
    # Replace with actual private key
    # NEVER commit actual keys to version control
    -----END PRIVATE KEY-----
