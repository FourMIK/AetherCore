# Example secrets.env file for kustomize secretGenerator
# DO NOT COMMIT THIS FILE WITH ACTUAL VALUES
#
# Usage:
#   1. Copy this template: cp secrets.env.example secrets.env
#   2. Generate secure values: openssl rand -base64 32
#   3. Fill in the values below
#   4. Add secrets.env to .gitignore
#   5. Deploy: kustomize build . | kubectl apply -f -

# PostgreSQL password
POSTGRES_PASSWORD=

# JWT signing secret
JWT_SECRET=

# MinIO root password
MINIO_ROOT_PASSWORD=

# Grafana admin password
GRAFANA_ADMIN_PASSWORD=

# Constructed database URL
DATABASE_URL=postgresql://aethercore:YOUR_POSTGRES_PASSWORD@postgres:5432/aethercore
